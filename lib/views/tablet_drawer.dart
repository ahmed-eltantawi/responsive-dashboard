import 'package:flutter/material.dart';

class TabletDrawerView extends StatelessWidget {
  const TabletDrawerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
